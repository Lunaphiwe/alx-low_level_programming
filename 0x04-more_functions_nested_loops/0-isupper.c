#include "main.h"

/**
 * _isupper - check for uppercase alphabets
 * @c: input character
 * Return: (1) if uppercase and (0) otherwise
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else if
	{
		return (0);
	}
}
